<!DOCTYPE html>
<html lang="ar" dir="rtl">
<head>
<meta charset="UTF-8">
<meta name="viewport" content="width=device-width, initial-scale=1.0">
<title>القنصلية السورية 2026</title>
<link href="https://fonts.googleapis.com/css2?family=Roboto:wght@400;500;700&display=swap" rel="stylesheet">
<link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css">
<style>
*{margin:0;padding:0;box-sizing:border-box;}
body{font-family:'Roboto',sans-serif; background:#fffbe6; transition:0.5s; overflow-x:hidden;}
body.dark-mode{background:#1e1e1e;color:#eee;}
header{background: linear-gradient(90deg,#a80000,#8b0000); color:#fff; padding:20px; text-align:center; position:relative; transition:0.5s;}
header img{ max-height:80px; margin:0 auto 10px; display:block; }
header h1{ font-size:28px; margin:0; }
.toggle-btn{ position:absolute; top:15px; left:15px; padding:8px 15px; border:none; border-radius:25px; background:#444; color:#fff; cursor:pointer; transition:0.3s; }

/* خلفية متحركة */
body::before{content:""; position:fixed; top:0; left:0; width:100%; height:100%; background: linear-gradient(120deg,#ff9a9e,#fad0c4,#a18cd1,#fbc2eb); background-size:400% 400%; animation: gradientBG 15s ease infinite; z-index:-1;}
@keyframes gradientBG{0%{background-position:0% 50%;}50%{background-position:100% 50%;}100%{background-position:0% 50%;}}

/* شاشة الترحيب */
#welcome-screen {
  position:fixed;
  top:0; left:0;
  width:100%; height:100%;
  background: rgba(0,0,0,0.85);
  color:#fff;
  display:flex;
  justify-content:center;
  align-items:center;
  flex-direction:column;
  font-size:22px;
  z-index:9999;
  text-align:center;
  animation: fadeIn 0.5s ease-in-out;
}
@keyframes fadeIn { from{opacity:0;} to{opacity:1;} }
#welcome-text {
  border-right: 3px solid #fff;
  white-space: nowrap;
  overflow: hidden;
  width: 0;
  animation: typing 3s steps(40,end) forwards;
}
@keyframes typing {
  from { width: 0 }
  to { width: 100% }
}

/* صفحة تسجيل الدخول */
#login{display:flex; justify-content:center; align-items:center; height:100vh; background: url('https://images.unsplash.com/photo-1507525428034-b723cf961d3e?auto=format&fit=crop&w=1950&q=80') no-repeat center center/cover; position: relative; overflow: hidden;}
#login::before{content: ""; position: absolute; top:0; left:0; width:100%; height:100%; background: rgba(0,0,0,0.5); z-index: 0;}

/* فقاعات متحركة */
.bubbles span{position:absolute; bottom:-50px; width:20px; height:20px; background: rgba(255,255,255,0.3); border-radius:50%; animation: rise 10s infinite ease-in;}
.bubbles span:nth-child(1){ left:10%; animation-duration:8s; width:15px; height:15px; }
.bubbles span:nth-child(2){ left:20%; animation-duration:12s; width:25px; height:25px; }
.bubbles span:nth-child(3){ left:30%; animation-duration:10s; }
.bubbles span:nth-child(4){ left:40%; animation-duration:14s; width:18px; height:18px; }
.bubbles span:nth-child(5){ left:50%; animation-duration:11s; }
.bubbles span:nth-child(6){ left:60%; animation-duration:13s; width:22px; height:22px; }
.bubbles span:nth-child(7){ left:70%; animation-duration:9s; width:16px; height:16px; }
.bubbles span:nth-child(8){ left:80%; animation-duration:15s; width:28px; height:28px; }
.bubbles span:nth-child(9){ left:90%; animation-duration:12s; width:20px; height:20px; }
.bubbles span:nth-child(10){ left:95%; animation-duration:10s; width:18px; height:18px; }
@keyframes rise{0%{ transform: translateY(0) scale(1); opacity:0;}50%{ opacity:0.7;}100%{ transform: translateY(-1200px) scale(1.2); opacity:0;}}

.login-box{background: rgba(255,255,255,0.2); backdrop-filter: blur(20px); padding:50px 40px; border-radius:25px; box-shadow:0 15px 35px rgba(255,255,255,0.4); width:350px; text-align:center; transform:scale(0.8); opacity:0; animation: loginFade 1s forwards; transition: transform 0.3s, box-shadow 0.3s; position:relative; z-index:1; border:1px solid rgba(255,255,255,0.3);}
.login-box:hover{transform: scale(1); box-shadow:0 20px 50px rgba(255,255,255,0.6);}
@keyframes loginFade{to{opacity:1; transform:scale(1);}}
.login-box h2{color:#fff; margin-bottom:25px; font-weight:700; text-shadow:0 2px 8px rgba(0,0,0,0.7);}
.input-group{position:relative;margin:15px 0;}
.input-group input{width:100%; padding:12px 40px; border:none; border-radius:12px; outline:none; font-size:16px; transition:0.3s; background:rgba(255,255,255,0.3); color:#fff;}
.input-group input:focus{ box-shadow:0 0 12px #ff4d4d; background:rgba(255,255,255,0.6); color:#222;}
.input-group i{ position:absolute; top:50%; transform:translateY(-50%); right:12px; font-size:18px; color:#fff; transition:0.3s;}
.input-group input:focus + i{ color:#ff4d4d; transform:translateY(-50%) scale(1.2); }

.login-box button{width:100%; padding:12px; margin-top:15px; border:none; border-radius:12px; font-size:16px; font-weight:600; cursor:pointer; background: linear-gradient(90deg,#a80000,#ff4d4d); background-size:200% 200%; animation: gradientBtn 3s ease infinite; color:#fff; transition:0.3s;}
.login-box button:hover{ transform: scale(1.05); box-shadow:0 5px 20px rgba(255,77,77,0.6);}
@keyframes gradientBtn{0%{background-position:0% 50%;}50%{background-position:100% 50%;}100%{background-position:0% 50%;}}

/* المحتوى */
#content{ display:none; }
.container{ max-width:1100px; margin:auto; padding:30px 20px; }
.intro{text-align:center; margin-bottom:40px; font-size:18px; line-height:1.6;}
.services{ display:grid; grid-template-columns:repeat(auto-fit,minmax(260px,1fr)); gap:20px; }
.service{ background: linear-gradient(135deg,#fff,#eee); padding:20px; border-radius:20px; box-shadow:0 4px 15px rgba(0,0,0,0.1); transition:0.5s; text-align:center; font-weight:500; cursor:pointer; color:#222; animation: servicePulse 3s infinite alternate;}
@keyframes servicePulse{0%{transform:scale(1);}50%{transform:scale(1.03);}100%{transform:scale(1);}}
.service:hover{ transform:translateY(-5px) scale(1.05); box-shadow:0 6px 20px rgba(0,0,0,0.2); }
.service img{ width:80px; margin-bottom:15px; transition:0.3s; }
.service h3{ color:#a80000; margin-bottom:8px; }
.whatsapp{text-align:center; margin:40px 0;}
.whatsapp a{background:#25D366; color:white; padding:12px 30px; border-radius:30px; text-decoration:none; font-weight:bold; transition:0.3s;} 
.whatsapp a:hover{ background:#1ebe57; }
footer{ background:#222; color:#fff; text-align:center; padding:15px; margin-top:40px; font-size:14px; position:relative; overflow:hidden; }
.dynamic-text{text-align:center; padding:15px; font-size:18px; font-weight:bold; transition:color 1s;}

/* زر واتساب عائم */
#floating-whatsapp {
  position: fixed;
  bottom: 20px;
  right: 20px;
  background: #25D366;
  color: white;
  font-size: 28px;
  padding: 15px;
  border-radius: 50%;
  box-shadow: 0 4px 15px rgba(0,0,0,0.3);
  text-align: center;
  z-index: 999;
  transition: 0.3s;
}
#floating-whatsapp:hover {
  background: #1ebe57;
  transform: scale(1.1);
  box-shadow: 0 6px 20px rgba(0,0,0,0.4);
}
</style>
</head>
<body>

<!-- شاشة الترحيب -->
<div id="welcome-screen">
  <div id="welcome-text">مرحباً بكم في موقع القنصلية السورية - من صاحب الموقع أبو عزام</div>
</div>

<div id="login">
  <div class="bubbles">
    <span></span><span></span><span></span><span></span><span></span>
    <span></span><span></span><span></span><span></span><span></span>
  </div>
  <div class="login-box">
    <img src="https://upload.wikimedia.org/wikipedia/commons/thumb/5/53/Coat_of_arms_of_Syria.svg/100px-Coat_of_arms_of_Syria.svg.png" alt="شعار">
    <h2>تسجيل الدخول</h2>
    <div class="input-group">
      <input type="text" id="username" placeholder="اسم المستخدم" maxlength="30">
      <i class="fa fa-user"></i>
    </div>
    <div class="input-group">
      <input type="password" id="password" placeholder="كلمة المرور" maxlength="30">
      <i class="fa fa-lock"></i>
    </div>
    <button onclick="login()">دخول</button>
  </div>
</div>

<div id="content">
  <header>
    <button class="toggle-btn" onclick="toggleDarkMode()">🌙 تبديل الوضع</button>
    <img src="p.ping" alt="شعار" class="header-logo">
    <h1>القنصلية السورية</h1>
  </header>
  <div class="container">
    <div class="intro">
      <p>أهلاً بكم في الموقع الرسمي للقنصلية السورية. نعمل على خدمة المواطنين السوريين عبر كافة المعاملات القنصلية من إصدار وتجديد جوازات السفر، وتصديق الأوراق، وتقديم الاستشارات.</p>
    </div>
    <div class="services">
      <div class="service" id="service1"><img src="https://cdn-icons-png.flaticon.com/512/3135/3135715.png"><h3>حجز معاملات</h3><p>إمكانية الحجز المسبق لمعاملاتكم القنصلية بسهولة.</p></div>
      <div class="service" id="service2"><img src="https://cdn-icons-png.flaticon.com/512/2910/2910761.png"><h3>خدمات الجوازات</h3><p>إصدار وتجديد جوازات السفر بأسرع وقت.</p></div>
      <div class="service" id="service3"><img src="https://cdn-icons-png.flaticon.com/512/3091/3091676.png"><h3>توثيق الأوراق</h3><p>توثيق كافة الأوراق الرسمية والمستندات القانونية.</p></div>
      <div class="service" id="service4"><img src="https://cdn-icons-png.flaticon.com/512/2910/2910762.png"><h3>استشارات قانونية</h3><p>إرشادات قانونية ومعاملات خاصة بالمغتربين.</p></div>
    </div>
    <div class="whatsapp"><a href="https://wa.me/13433075506" target="_blank" rel="noopener noreferrer">📱 تواصل عبر واتس آب</a></div>
  </div>
  <footer>
    المطور أبو يعقوب 💻
    <p>© 2026 القنصلية السورية - جميع الحقوق محفوظة</p>
    <p class="dynamic-text" id="dynamicText">أهلاً بكم في موقع القنصلية السورية!</p>
  </footer>
</div>

<!-- زر واتساب عائم -->
<a href="https://wa.me/13433075506" 
   target="_blank" 
   rel="noopener noreferrer" 
   id="floating-whatsapp">
   <i class="fab fa-whatsapp"></i>
</a>

<script>
function toggleDarkMode(){document.body.classList.toggle("dark-mode");}

// شاشة الترحيب تختفي بعد 3.5 ثواني
setTimeout(()=>{
  document.getElementById("welcome-screen").style.display="none";
},3500);

// تسجيل دخول آمن
function login(){
  const user=document.getElementById("username").value.replace(/<[^>]*>?/gm, '').trim();
  const pass=document.getElementById("password").value.replace(/<[^>]*>?/gm, '').trim();
  if(user==="admin" && pass==="1234"){
    document.getElementById("login").style.display="none";
    document.getElementById("content").style.display="block";
  } else { alert("❌ اسم المستخدم أو كلمة المرور غير صحيحة"); }
}

// نص ديناميكي
const colors=["#a80000","#ff8c00","#25D366","#1e90ff","#ff1493"];
let index=0;
setInterval(()=>{
  document.getElementById("dynamicText").style.color=colors[index];
  index=(index+1)%colors.length;
},2000);

// خلفية الخدمات متغيرة
const serviceIds=["service1","service2","service3","service4"];
setInterval(()=>{
  serviceIds.forEach(id=>{
    document.getElementById(id).style.background=`linear-gradient(135deg, ${colors[Math.floor(Math.random()*colors.length)]}, ${colors[Math.floor(Math.random()*colors.length)]})`;
  });
},3000);
</script>
</body>
</html>
